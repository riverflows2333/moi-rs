# moi-rs

A Rust workspace aiming to recreate MathOptInterface's layered abstractions with strong typing and zero-cost abstractions.

MVP focus:
- Core types (indices, errors, sets, functions)
- In-memory model implementing a minimal `ModelLike`
- Solver API traits and statuses

Build:
```sh
cargo build
```
