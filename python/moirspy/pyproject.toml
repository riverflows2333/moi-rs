[build-system]
requires = ["maturin>=1.11,<2.0"]
build-backend = "maturin"

[project]
name = "moirspy"
description = "A solver-independent optimization modeling API implemented in Rust"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
]
dynamic = ["version"]

[project.urls]
Homepage = "https://github.com/riverflows2333/moi-rs"
Repository = "https://github.com/riverflows2333/moi-rs"
Issues = "https://github.com/riverflows2333/moi-rs/issues"
